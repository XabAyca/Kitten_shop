<h4 id='list-orders'>Liste des commandes (<%=@orders.count%>)</h4>

<div class="table-responsive mb-2">
  <table class="table table-striped table-sm">
    <thead>
      <tr>
        <th>ID</th>
        <th>Email utilisateur</th>
        <th>Objects</th>
        <th>Prix total</th>
        <th>date</th>
        <th>Supprimer</th>
      </tr>
    </thead>
    <tbody>
        <%@orders.each do |order|%>
          <tr id='order<%=order.id%>'>
            <td><%=order.id%></td>
            <td><%=order.user.email%></td>
            <td><%=order.items.count%></td>
            <td><%=order.total %> € </td>
            <td><%=order.created_at.strftime('%d/%m/%Y')%></td>
            <td><a href=<%=admin_order_path(order.id)%> data-method='delete' data-confirm='Valider la suppression ?' data-remote='true' class='btn btn__secondary btn-sm' %>Supprimer</a></td>
          </tr>  
        <%end%>
    </tbody>
  </table>
</div>
<div class=" mb-5">
  <%=render 'admin/admin/back_btn'%>
<div>