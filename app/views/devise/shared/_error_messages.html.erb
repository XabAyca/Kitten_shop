<% if resource.errors.any? %>
<script>
let i = 0  
let btn_flash = document.getElementsByClassName("btn-close");
let flash_block = document.getElementsByClassName("alert");
</script>

  <div id="error_explanation">
    <h2>
      <% I18n.t("errors.messages.not_saved",
                 count: resource.errors.count,
                 resource: resource.class.model_name.human.downcase)
       %>
    </h2>
    <ul>
    <% resource.errors.full_messages.each do |message| %>
  <div id="flash-block" class="alert mb-0 <%= bootstrap_class_for_flash("error") %> alert-dismissible" role="alert">
    <%= message %>
  </div>
<% end %>
     
    </ul>
  </div>
<% end %>


