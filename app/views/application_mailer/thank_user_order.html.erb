<!DOCTYPE html>
<html>
  <head>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  </head>
  <body>
    <h1>Merci, <%= @order.user.email %></h1>
    <p>
        Votre commande n° <%= @order.id %>
    </p>
    <%@order.items.each do |item|%>
      <div>
        <% if item.item_image.attached? %>
          <%= image_tag item.item_image, style: "width: 50px", :crop => :fill %>
        <% else %>
          <%= image_tag item.image_url, style: "width: 50px", :crop => :fill %>
        <% end %>
      </div>
    <% end %>
  </body>
</html>