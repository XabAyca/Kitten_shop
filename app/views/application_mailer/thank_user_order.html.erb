<!DOCTYPE html>
<html>
  <head>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  </head>
  <body>
    <h1>Merci, <%= @order.user.email %></h1>
    <p>
        Votre commande n° <%= @order.id %>
    </p>
   
    <%@order.items.each do |item| %>
      <div>
      <%= "https://thp-catstore.herokuapp.com/rails/active_storage/blobs/redirect/#{item.item_image.blob.key}/#{item.item_image.blob.filename}"%>
      <%= email_image_tag("#{item.item_image.blob.key}/#{item.item_image.blob.filename}") %>
    </div>
    <% end %>
  </body>
</html>