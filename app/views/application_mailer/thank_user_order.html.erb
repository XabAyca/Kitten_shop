<!DOCTYPE html>
<html>
  <head>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  </head>
  <body>
    <h1>Merci, <%= @order.user.email %></h1>
    <p>
        Votre commande n° <%= @order.id %>
    </p>
    
    <%@order.items.each do |item| %>
     
	  <%= image_tag url_for(order.item.image_url) %>
	   <%= image_tag url_for(order.item.item_image) %>
    <% end %>

  </body>
</html>